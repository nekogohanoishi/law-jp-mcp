{
  "buildCommand": "npm run build",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/$1" }
  ],
  "functions": {
    "api/[transport]/route.ts": {
      "maxDuration": 60
    }
  }
}
